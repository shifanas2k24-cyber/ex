name: Test Secrets

on:
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout
      uses: actions/checkout@v3

    - name: Use Secret
      run: echo "Using secret safely"
      env:
        API_KEY: ${{ secrets.API_KEY }}
